/// <reference types="vite/client" />

/**
 * Replaced by Vite at compile time. Standard production builds define this
 * as `false`; dev and explicit Lab builds define it as `true`.
 */
declare const __LAB_FRONTEND_INCLUDED__: boolean;
